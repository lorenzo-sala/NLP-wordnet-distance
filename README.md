# NLP-wordnet-distance

Three different NLP projects:

1) Semantic distance based on WordNet metrics;
2) Using N-grams to model simple tweets from public personalities (next token prediction);
3) FrameNet to WordNet mapping using manual annotation.
